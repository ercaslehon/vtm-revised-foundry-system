<div class="vtm-chat-card resource-card">
  <h3>{{localize "VTM_REVISED.Chat.ResourceChange"}}</h3>
  <p><strong>{{actor.name}}</strong></p>
  <p>{{resourcePath}}: {{oldValue}} → {{newValue}} ({{delta}})</p>
  {{#if reason}}<p>{{reason}}</p>{{/if}}
</div>
